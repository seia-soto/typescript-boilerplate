{
  "license": "MIT",
  "name": "now",
  "version": "0.0.1",
  "main": "built/index.js",
  "author": "Seia-Soto <seia@outlook.kr>",
  "private": true,
  "engines": {
    "node": ">= 14.16 <15"
  },
  "scripts": {
    "build": "swc src --out-dir built --sync && tsc --pretty --skipLibCheck --noEmit",
    "build:watch": "concurrently 'swc src -w --out-dir built --sync' 'tsc -w --pretty --skipLibCheck --noEmit'",
    "release": "standard-version",
    "lint": "eslint --ext .js,.ts src"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.46",
    "@swc/core": "^1.2.66",
    "@typescript-eslint/eslint-plugin": "^4.28.4",
    "@typescript-eslint/parser": "^4.28.4",
    "chokidar": "^3.5.2",
    "concurrently": "^6.2.0",
    "eslint": "^7.31.0",
    "eslint-config-standard-with-typescript": "^20.0.0",
    "eslint-plugin-import": "^2.23.4",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^5.1.0",
    "standard-version": "^9.3.1",
    "typescript": "^4.3.5"
  }
}
